# Normalize line endings to LF for the repo and on checkout
* text=auto eol=lf

# Explicitly declare text files (optional, helps with diffs)
*.ts text
*.html text
*.css text
*.json text
*.md text
*.yml text
*.yaml text
